Neuron is a test with qubit layers that are encased at certain intervals to keep from having issues, then instead of laid on top its stacked in matrices with interconnected neural strings. These strings mimic string theory.
